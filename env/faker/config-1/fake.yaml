# error, info, debug
server:
  host: "0.0.0.0"
  port: "12000"

mode: "debug"

handlers:
- path: "/ping"
  methods:
  - "GET"
  must_params:
  must_kv_params:
  response:
    status_code: 200
    source: "text"
    headers:
      "content-type": "text/plain"
      "fake-region": "us-east"
    args:
      content: "pong"
